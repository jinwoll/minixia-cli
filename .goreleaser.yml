version: 3

project_name: minixia

builds:
  - id: minixia
    main: ./main.go
    binary: minixia
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/jinwoll/minixia-cli/cmd.version={{.Version}}
      - -X github.com/jinwoll/minixia-cli/cmd.commit={{.ShortCommit}}
      - -X github.com/jinwoll/minixia-cli/cmd.date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: default
    format: binary
    # 与 install.sh / install.ps1 / upgrade 约定一致：amd64 → x86_64
    name_template: 'minixia-{{ .Os }}-{{ if eq .Arch "amd64" }}x86_64{{ else }}{{ .Arch }}{{ end }}'

checksum:
  name_template: "checksums.sha256"

release:
  github:
    owner: jinwoll
    name: minixia-cli

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
